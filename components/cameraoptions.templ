package components

templ OverallCameraComponent(s *ScrcpyInfo) {
  <p> { s.DeviceName }</p>
  <ul>
  for _, v := range s.Cameras {
    <li>{ v.ID }</li>
    <li>{ v.Position }</li>
    for _, y := range v.Sizes {
        <li>{ y.Resolution }</li>
      }
    }
  </ul>
}
