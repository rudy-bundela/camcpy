// components/index.templ
package components

templ Welcome() {
	@Layout(pairForm(), CodePen([]string{"Waiting for input..."}), CarouselComponent())
}

templ CarouselComponent() {
	<div class="justify-end">
		<button class="btn btn-soft btn-wide">Pairing</button>
	</div>
}
